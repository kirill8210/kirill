 
    <div id="re<%=project.id%>" style="outline:1px solid red;width:400px; margin:auto;" > 
    <%= best_in_place project, :name, as: :input, activator: "#edit-project-name-#{project.id}" %>

    <%= link_to "#", class: 'btn btn-primary', id: "edit-project-name-#{project.id}" do %>
            <i class="fa fa-pencil"></i>
            <% end %>
    <%= link_to  project, class: "btn btn-danger", remote: true, method: :delete do %>
            <i class="fa fa-trash-o"></i>
            <% end %>
    <br>
    <%= form_for([project, Task.new], :remote => true) do |f| %>  
    <div >
    <br>
    <%= f.text_field :name %><%= f.submit "Create task", class: 'btn btn-success' %>
    </div>
    <div class="actions">
    <% end %>
    <ul class="sortable" id="task<%=project.id%>" style="list-style:none">
    <% project.tasks.each do |task| %>
    <%= render 'tasks/task', :task => task %>
    <% end %>
    </ul>
    </div>
    </div>
    

    